@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Fira+Code:wght@400;500&display=swap');

@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  * {
    @apply border-slate-200;
  }
  body {
    @apply bg-slate-50 text-slate-900 antialiased;
  }
}

@layer components {
  .btn-primary {
    @apply px-6 py-3 bg-primary-600 hover:bg-primary-700 text-white font-semibold rounded-lg transition-all duration-200 shadow-lg hover:shadow-xl active:scale-95;
  }
  
  .btn-secondary {
    @apply px-6 py-3 bg-white hover:bg-slate-50 text-slate-700 font-semibold rounded-lg border-2 border-slate-200 transition-all duration-200 hover:border-primary-300 active:scale-95;
  }
  
  .card {
    @apply bg-white rounded-xl shadow-md border border-slate-200 p-6 transition-all duration-200 hover:shadow-lg;
  }
  
  .input-field {
    @apply w-full px-4 py-3 border-2 border-slate-200 rounded-lg focus:border-primary-500 focus:outline-none focus:ring-2 focus:ring-primary-100 transition-all;
  }
  
  .sidebar-link {
    @apply flex items-center gap-3 px-4 py-3 rounded-lg text-slate-600 hover:bg-primary-50 hover:text-primary-700 transition-all duration-200 cursor-pointer;
  }
  
  .sidebar-link.active {
    @apply bg-primary-100 text-primary-700 font-semibold;
  }
}

.gradient-bg {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.stats-card {
  @apply bg-gradient-to-br from-primary-500 to-primary-700 text-white rounded-xl p-6 shadow-lg;
}

.chart-container {
  @apply bg-white rounded-xl shadow-md border border-slate-200 p-6;
}

.upload-zone {
  @apply border-2 border-dashed border-slate-300 rounded-xl p-8 text-center bg-slate-50 hover:bg-slate-100 hover:border-primary-400 transition-all cursor-pointer;
}

.upload-zone.dragover {
  @apply border-primary-500 bg-primary-50;
}

.algorithm-card {
  @apply bg-white border-2 border-slate-200 rounded-xl p-5 cursor-pointer transition-all hover:border-primary-400 hover:shadow-lg;
}

.algorithm-card.selected {
  @apply border-primary-500 bg-primary-50 shadow-lg;
}

.metric-badge {
  @apply inline-flex items-center px-3 py-1 rounded-full text-sm font-medium;
}

.loader {
  border: 3px solid #f3f4f6;
  border-top: 3px solid #0ea5e9;
  border-radius: 50%;
  width: 40px;
  height: 40px;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.fade-in {
  animation: fadeIn 0.5s ease-in;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}
